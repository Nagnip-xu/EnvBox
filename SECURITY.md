# EnvBox 安全说明

EnvBox 能够修改 Windows 注册表、PATH 和 SDK 配置，因此应当被视为高权限本地工具。

## 使用建议

- 修改系统变量前确认应用来源可信，并检查即将修改的变量和路径。
- 不要导入来源不明的环境备份文件。
- 快照与导出文件可能包含令牌、密码、连接字符串等敏感信息，应存放在仅当前用户可访问的位置。
- 快照采用明文 JSON，产品当前不提供也不计划默认启用快照加密；磁盘加密、Windows 账户隔离和备份介质保护由使用者负责。
- 不要将 `env-backup-*.txt`、`.env`、快照或诊断日志提交到公开仓库。
- 对外部安装的 SDK，优先使用官方卸载器；EnvBox 不会在无法确认所有权时强制删除目录。

## 安全边界

- 普通用户模式只应写入用户级注册表。
- 系统变量恢复和写入需要管理员权限。
- 所有高风险写操作必须先创建快照。
- 创建快照时会严格读取用户级和系统级注册表；任一作用域无法完整枚举时操作立即失败，不会把空数据误当成有效备份。
- `%AppData%\EnvBox` 首次创建快照时会移除继承权限，仅向当前用户、SYSTEM 和本机 Administrators 授予完全控制。权限加固失败时不会继续创建快照。
- 快照包含 `schemaVersion` 与应用版本；不支持的未来格式、损坏文件和文件名/内部 ID 不一致都会被拒绝。旧版无 schema 快照仍按 v1 兼容读取。
- 快照恢复和批量导入失败时会尝试自动回滚，并报告回滚是否完整。
- 恢复与导入的选择项会在 Rust 后端重新校验，并从原快照/导入文件派生值，前端不能提交任意替代值。
- 导入文件最大为 5 MiB，且必须使用受支持的数据版本。
- 快照 ID 和环境变量名会在 Rust 后端再次校验，不能只依赖前端。
- 安装清单使用原子替换写入并记录 EnvBox 发起的包管理器任务。只有能确认归属时才据此清理安装目录；外部安装仍优先调用官方卸载器。
- 可取消任务仅限 EnvBox 能直接跟踪到 PID 的普通子进程。已交给提权/官方卸载器的独立进程可能无法从当前窗口取消。

## 报告问题

请勿在公开问题中附带真实环境导出、完整快照、令牌或个人路径。报告时请提供脱敏后的复现步骤、EnvBox 版本、Windows 版本和相关错误信息。
